# Project Instructions

## Актуальная документация

Всегда используй Context7, когда нужен актуальный справочник по библиотеке или API, примеры кода, шаги установки или конфигурации. Для этого сначала вызывай `resolve-library-id`, затем `query-docs`, даже если пользователь явно не написал `use context7`.

## Универсальный импорт расчетных листов

Цель проекта — создать максимально универсальный механизм считывания и нормализации расчетных листов.

- 1С:ЗУП — наиболее часто встречающийся и приоритетный для поддержки формат расчетных листов, который следует использовать как основной практический сценарий и источник тестовых примеров.
- При этом не завязывать импорт, распознавание и внутреннюю модель данных исключительно на 1С:ЗУП, ее названия разделов, структуру выгрузки или конкретный вариант расчетного листка.
- Поддерживать расчетные листы из разных учетных систем и источников при наличии достаточных данных для определения периода, начислений, выплат, удержаний и иных расчетных показателей.
- Термины и структуры конкретного источника преобразовывать в единую внутреннюю модель на уровне адаптеров и правил распознавания.
- Новый функционал импорта проектировать как независимый от поставщика исходных документов, сохраняя полноценную и приоритетную поддержку расчетных листов 1С:ЗУП.
- Расчеты требований, индексации и материальной ответственности строить по реконструированным денежным потокам, восстановленным из расчетных листков и методологии расчета, а не по фактическим банковским выплатам. Фактические выплаты использовать только как источник данных для реконструкции, если это явно предусмотрено логикой.

### Построчное распознавание без суммирования

- Распознавание и нормализация расчетных листков должны сохранять каждую исходную позицию отдельной строкой, даже если у нескольких позиций совпадают название, период, категория, сумма или дата выплаты.
- На этапе импорта запрещено неявно объединять, дедублицировать или суммировать начисления, выплаты, удержания, отпуска, премии и иные позиции. Исключение допустимо только для доказанного технического дубля одной и той же исходной строки; такое решение должно сохранять трассировку и быть покрыто тестом.
- Сводные суммы и группировки формируются только на последующих расчетных слоях. Они не заменяют и не изменяют нормализованный построчный слой, из которого всегда можно восстановить исходный состав суммы.
- Если в одном расчетном листке присутствуют несколько однотипных выплат с разными датами или периодами, они обязательно остаются отдельными позициями, поскольку каждая из них может образовывать самостоятельную базу и срок для расчета по статье 236 ТК РФ.

## Таблица и Docs-расшифровка

- Google Sheets остается основным рабочим местом расчета: функции пересчета должны записывать итоговые суммы и рабочие значения обратно в существующую структуру таблицы, не создавая параллельную замену без явного запроса.
- Google Docs используется для раскрытия и текстовой расшифровки расчета по уже рассчитанным данным таблицы: формулы, дневные/помесячные строки, методология, пояснения и итоговая судебная narrative-структура.
- Нельзя подменять расчет в таблице генерацией Docs: если пользователь просит пересчитать вынужденный прогул, материальную ответственность или накопленные отпуска, сначала обновляются соответствующие ячейки таблицы, а Docs только детализирует результат.
- Нельзя очищать или перестраивать весь Google Doc без явного запроса. Автоматически обновляемые фрагменты должны быть ограничены маркерами или иным локальным диапазоном, чтобы сохранять существующую структуру документа.

## Утвержденная структура судебного расчета

- Эталоном структуры и оформления судебного расчета является согласованная с пользователем копия `/Users/aegorfk/Desktop/Расчеты.docx` с SHA-256 `f59e6c85ea0c482fe44e159a21e1b3d22f0429008e9adafd7005b69a4d98b298`.
- Канонический Google Docs-шаблон этого расчета имеет ID `1qwMjRD99FNWnF2Wu8T7wbkSuvDOiH5tu82aSxovxArE`. Каждая новая версия создается непосредственно из него; предыдущий сгенерированный расчет нельзя автоматически превращать в новый шаблон. Смена канонического шаблона требует явного согласования пользователя.
- Без отдельного предварительного согласования с пользователем запрещено менять утвержденные состав и порядок разделов, иерархию заголовков, названия и порядок колонок, визуальные стили, ширины, цвета, границы, числовые форматы и общую компоновку судебного расчета. Исправление расчетной логики должно встраиваться в этот макет без самовольного редизайна.
- Если функциональное изменение не помещается в утвержденную структуру, сначала нужно обновить спецификацию и показать пользователю предлагаемый макет; реализация изменения структуры допускается только после явного одобрения.
- Судебный расчет должен быть проверяемым: каждое итоговое требование раскрывается по сути нарушения и периоду, а его сумма должна однозначно складываться из детальных расчетных позиций. Техническая трассировка до источника хранится в данных, даже если номера строк и листов не показываются в пользовательском документе.
- Состав и суммы требований в судебном расчете и Google Docs должны строго соответствовать текущему выбору пользователя в разделе `Аудит и требования`: отмеченные позиции включаются, снятые позиции исключаются, итог равен сумме включенных позиций. При расхождении генерация итогового документа блокируется с понятным сообщением.
- Автоматически обновляемые диапазоны должны сохранять утвержденное форматирование окружающего документа и не создавать параллельный отчет с иной структурой.
